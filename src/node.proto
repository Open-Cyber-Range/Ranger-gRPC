syntax = "proto3";

import "src/common.proto";
import "google/rpc/status.proto";

message Node {
    string name = 1;
    string exerciseName = 2;
    string templateName = 3;
}

service NodeService {
    rpc create (Node) returns (Identifier, Status);
    rpc delete (Identifier) returns (SimpleResult);
}
