syntax = "proto3";

import "src/reply.proto";
import "src/identifier.proto";

message Node {
    string name = 1;
    string exerciseName = 2;
    string templateName = 3;
}

service NodeService {
    rpc create (Node) returns (Reply);
    rpc delete (Identifier) returns (Reply);
}
